version: '3.9'

services:
  ryanair-bot:
    build: .
    container_name: ryanair-flight-check-bot
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - .:/usr/src/app
    command: ["node", "bot.js"]
